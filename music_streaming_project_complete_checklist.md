# چک‌لیست جامع پروژه اپلیکیشن استریم موسیقی و شبکه اجتماعی

> این فایل برای مقایسه مستقیم نیازمندی‌های پروژه با پیاده‌سازی فعلی تهیه شده است.
> هر موردی که کامل شده است از `[ ]` به `[x]` تغییر داده شود.
> برای موارد ناقص می‌توان از `[~]` و برای موارد نامرتبط یا تصمیم‌گیری‌نشده از `[-]` استفاده کرد.

---

## 0) مشخصات کلی پروژه

- [x] اپلیکیشن یک پلتفرم **استریم موسیقی** است.
- [x] اپلیکیشن در کنار پخش موسیقی، امکانات یک **شبکه اجتماعی کوچک** دارد.
- [x] نام اختصاصی و خلاقانه‌ای برای اپلیکیشن انتخاب شده است.
- [x] رابط کاربری با `Jetpack Compose` پیاده‌سازی شده است.
- [x] معماری برنامه بر اساس `Clean Architecture` طراحی شده است.
- [x] جریان داده در رابط کاربری بر اساس `UDF` است.
- [x] عملیات ناهمگام با `Coroutines` و `Flow` مدیریت می‌شود.
- [x] پخش موسیقی در پس‌زمینه ادامه پیدا می‌کند.
- [~] کد از نظر خوانایی، نام‌گذاری، جداسازی مسئولیت‌ها و قابلیت نگهداری تمیز است.
- [x] وضعیت‌های رابط کاربری به‌شکل متمرکز و قابل پیش‌بینی مدیریت می‌شوند.
- [x] انیمیشن‌ها و ریزتعامل‌های رابط کاربری به‌صورت روان پیاده‌سازی شده‌اند.

---

# بخش اول: موارد بسیار حساس و دارای نمره مستقیم

## 1) دیزاین سیستم و حذف Hardcode

- [ ] برای رنگ‌های برنامه از مقادیر پراکنده و مستقیم داخل کامپوزبل‌ها استفاده نشده است.
- [~] تمام رنگ‌های اصلی، فرعی، پس‌زمینه، سطح، متن، خطا و حالت‌های مختلف در دیزاین سیستم تعریف شده‌اند.
- [~] اندازه فونت‌ها به‌صورت پراکنده و Hardcode در صفحات نوشته نشده است.
- [x] تمام سبک‌های متنی در `Typography` سفارشی برنامه تعریف شده‌اند.
- [~] فاصله‌ها، `Padding`ها و `Margin`های تکرارشونده در یک سیستم فاصله‌گذاری یا مقادیر مشترک تعریف شده‌اند.
- [x] گوشه‌ها و فرم اجزای رابط کاربری در `Shapes` سفارشی تعریف شده‌اند.
- [x] یک `MaterialTheme` سفارشی برای برنامه وجود دارد.
- [x] `MaterialTheme` سفارشی شامل `Colors` یا `ColorScheme` است.
- [x] `MaterialTheme` سفارشی شامل `Typography` است.
- [x] `MaterialTheme` سفارشی شامل `Shapes` است.
- [~] ظاهر دکمه‌ها، کارت‌ها، چیپ‌ها و ورودی‌ها در صفحات مختلف یکپارچه است.
- [x] کامپوننت‌های تکراری به‌صورت کامپوزبل مشترک ساخته شده‌اند.

### بررسی سریع Hardcode

- [~] در پروژه برای رنگ‌ها جستجوی `Color(0x` انجام شده و موارد پراکنده حذف یا توجیه شده‌اند.
- [~] در پروژه برای متن‌های مستقیم داخل `Text("...")` جستجو شده است.
- [~] در پروژه برای اندازه‌های تکراری مانند `16.dp`، `24.dp` و موارد مشابه بررسی انجام شده است.
- [~] مقادیر ثابت قابل اشتراک در محل مناسب متمرکز شده‌اند.

---

## 2) منابع متنی و Clean Code

- [ ] هیچ متن قابل مشاهده برای کاربر مستقیماً داخل کد نوشته نشده است.
- [ ] تمام متن‌های رابط کاربری از `strings.xml` خوانده می‌شوند.
- [~] متن دکمه‌ها در `strings.xml` قرار دارد.
- [~] متن عنوان صفحات در `strings.xml` قرار دارد.
- [x] متن خطاها در `strings.xml` قرار دارد.
- [x] متن Snackbarها در `strings.xml` قرار دارد.
- [x] متن Empty Stateها در `strings.xml` قرار دارد.
- [x] متن وضعیت‌های چت در `strings.xml` قرار دارد.
- [x] متن اعلان‌ها در منابع متنی تعریف شده است.
- [x] نام کلاس‌ها، توابع و متغیرها واضح و معنادار است.
- [~] هر کلاس فقط مسئولیت مشخصی دارد.
- [~] منطق تجاری داخل کامپوزبل‌ها نوشته نشده است.
- [x] عملیات شبکه و دیتابیس مستقیماً از UI فراخوانی نمی‌شود.
- [~] کدهای تکراری استخراج و بازاستفاده شده‌اند.
- [ ] فایل‌ها و کلاس‌های بسیار بزرگ به بخش‌های منطقی کوچک‌تر تقسیم شده‌اند.

---

## 3) دو زبانه بودن کامل برنامه

- [x] زبان فارسی به‌صورت کامل پشتیبانی می‌شود.
- [x] زبان انگلیسی به‌صورت کامل پشتیبانی می‌شود.
- [x] برای فارسی فایل منابع مناسب مانند `values-fa/strings.xml` وجود دارد.
- [x] برای انگلیسی فایل منابع پیش‌فرض یا `values-en/strings.xml` وجود دارد.
- [x] تمام صفحات اصلی در هر دو زبان ترجمه شده‌اند.
- [x] تمام صفحات فرعی در هر دو زبان ترجمه شده‌اند.
- [x] پیام‌های خطا در هر دو زبان ترجمه شده‌اند.
- [x] پیام‌های چت و وضعیت‌های سیستمی در هر دو زبان ترجمه شده‌اند.
- [x] متن اعلان پخش در هر دو زبان بررسی شده است.
- [x] با انتخاب فارسی، رابط کاربری به‌صورت خودکار `RTL` می‌شود.
- [x] با انتخاب انگلیسی، رابط کاربری به‌صورت خودکار `LTR` می‌شود.
- [~] ترتیب آیکون و متن در `RTL/LTR` درست تغییر می‌کند.
- [~] جهت فلش‌های بازگشت در `RTL/LTR` درست است.
- [~] چیدمان Top Bar در هر دو جهت درست دیده می‌شود.
- [~] Bottom Navigation در هر دو جهت به‌هم نمی‌ریزد.
- [~] لیست‌ها، کارت‌ها، دیالوگ‌ها و منوها در هر دو جهت تست شده‌اند.
- [x] تغییر زبان بدون به‌هم‌ریختن وضعیت برنامه انجام می‌شود.
- [x] زبان انتخابی در `DataStore` ذخیره می‌شود.
- [x] پس از بستن و بازکردن مجدد برنامه، زبان انتخابی حفظ می‌شود.

---

## 4) تم روشن، تاریک و سیستم

- [x] تم روشن پیاده‌سازی شده است.
- [x] تم تاریک پیاده‌سازی شده است.
- [x] گزینه پیروی از تم سیستم وجود دارد.
- [x] تغییر تم از داخل تنظیمات برنامه ممکن است.
- [x] تغییر تم سیستم در حالت «سیستم» روی برنامه اعمال می‌شود.
- [~] پالت رنگ تم روشن بهینه و خوانا است.
- [~] پالت رنگ تم تاریک بهینه و خوانا است.
- [~] کنتراست متن و پس‌زمینه در هر دو تم مناسب است.
- [~] کارت‌ها و سطوح در تم تاریک از پس‌زمینه قابل تشخیص‌اند.
- [~] رنگ آیکون‌ها در هر دو تم خوانا است.
- [~] رنگ وضعیت خطا، موفقیت و هشدار در هر دو تم مناسب است.
- [~] تصاویر، گرادیانت‌ها و Shimmer در هر دو تم بررسی شده‌اند.
- [x] تنظیم تم در `DataStore` ذخیره می‌شود.
- [x] پس از اجرای مجدد برنامه، تم انتخابی حفظ می‌شود.

---

## 5) آیکون اختصاصی برنامه

- [x] از آیکون پیش‌فرض اندروید استفاده نشده است.
- [ ] برنامه دارای `Adaptive Icon` اختصاصی است.
- [~] لایه Foreground آیکون اختصاصی تعریف شده است.
- [~] لایه Background آیکون اختصاصی تعریف شده است.
- [~] آیکون روی لانچرهای مختلف به‌درستی نمایش داده می‌شود.
- [x] نام و هویت بصری آیکون با برند برنامه هماهنگ است.

---

# بخش دوم: معماری و زیرساخت فنی

## 6) Clean Architecture و لایه‌بندی

- [x] پروژه حداقل به سه لایه `UI`، `Domain` و `Data` تقسیم شده است.
- [x] لایه `UI` شامل Screenها، Componentها و ViewModelها است.
- [~] لایه `Domain` شامل منطق تجاری، مدل‌های دامنه و UseCaseها است.
- [x] لایه `Data` شامل منبع شبکه، دیتابیس، کش و پیاده‌سازی Repositoryها است.
- [ ] رابط Repositoryها در محل مناسب تعریف شده است.
- [ ] پیاده‌سازی Repositoryها از رابط‌های آن‌ها جدا است.
- [x] UI به جزئیات مستقیم شبکه یا دیتابیس وابسته نیست.
- [x] منطق پخش، دانلود، Premium و انتخاب فایل محلی در لایه مناسب قرار دارد.
- [ ] مدل‌های شبکه مستقیماً به UI نشت نکرده‌اند یا برای آن‌ها Mapper وجود دارد.
- [~] مدل‌های دیتابیس مستقیماً به UI نشت نکرده‌اند یا برای آن‌ها Mapper وجود دارد.
- [~] خطاها در لایه‌های مختلف به مدل قابل مدیریت برای UI تبدیل می‌شوند.

### ساختار قابل بررسی پروژه

- [x] پکیج یا ماژول `ui` مشخص است.
- [x] پکیج یا ماژول `domain` مشخص است.
- [x] پکیج یا ماژول `data` مشخص است.
- [~] بخش `di` برای ماژول‌های تزریق وابستگی وجود دارد.
- [x] بخش `database` یا `local` مشخص است.
- [~] بخش `network` یا `remote` مشخص است.
- [~] بخش `repository` مشخص است.
- [x] بخش `usecase` مشخص است.
- [x] بخش `player` یا `media` مشخص است.
- [x] بخش `worker` برای دانلودها مشخص است.

---

## 7) الگوی UI، UDF و UiState

- [x] برای صفحات از `MVVM` یا `MVI` استفاده شده است.
- [x] جریان داده در صفحات یک‌طرفه است.
- [~] وضعیت هر صفحه در یک `data class` با نام یا مفهوم `UiState` تجمیع شده است.
- [x] UI وضعیت صفحه را از `StateFlow` دریافت می‌کند.
- [x] UI وضعیت‌ها را با روش Lifecycle-aware جمع‌آوری می‌کند.
- [x] کامپوزبل‌ها مستقیماً State داخلی پراکنده و ناسازگار ندارند.
- [x] وضعیت Loading داخل `UiState` مشخص است.
- [x] وضعیت Success یا داده اصلی داخل `UiState` مشخص است.
- [x] وضعیت Error داخل `UiState` مشخص است.
- [x] وضعیت Empty در صورت نیاز داخل `UiState` مشخص است.
- [x] وضعیت فیلترها و Query جستجو در State مدیریت می‌شود.
- [x] وضعیت پخش جاری از منبع واحد و قابل مشاهده می‌آید.

---

## 8) مدیریت Event و رویدادهای یک‌بارمصرف

- [~] تعاملات کاربر به‌شکل `Intent` یا `Event` به ViewModel ارسال می‌شوند.
- [~] کلیک Play یک Event مشخص دارد.
- [~] کلیک Pause یک Event مشخص دارد.
- [~] کلیک Like یک Event مشخص دارد.
- [x] کلیک Download یک Event مشخص دارد.
- [x] تغییر فیلتر جستجو یک Event مشخص دارد.
- [x] ارسال پیام یک Event مشخص دارد.
- [x] Follow و Unfollow از طریق Event مدیریت می‌شوند.
- [x] تغییر زبان و تم از طریق Event یا لایه تنظیمات مدیریت می‌شود.
- [x] رویدادهای یک‌بارمصرف با State معمولی اشتباه گرفته نشده‌اند.
- [x] Snackbarها با `SharedFlow` یا `Channel` مدیریت می‌شوند.
- [x] Navigationهای یک‌باره در صورت نیاز با Event مناسب مدیریت می‌شوند.
- [x] خطای شبکه بعد از چرخش یا Recomposition بی‌دلیل دوباره نمایش داده نمی‌شود.

---

## 9) Dependency Injection

- [x] یکی از `Hilt` یا `Koin` در پروژه استفاده شده است.
- [x] Repositoryها با DI ساخته و تزریق می‌شوند.
- [x] UseCaseها با DI ساخته و تزریق می‌شوند.
- [x] دیتابیس Room با DI فراهم می‌شود.
- [x] DAOها با DI فراهم می‌شوند.
- [x] کلاینت شبکه با DI فراهم می‌شود.
- [~] سرویس API با DI فراهم می‌شود.
- [x] DataStore با DI فراهم می‌شود.
- [x] وابستگی‌های پلیر و MediaSession با روش مناسب مدیریت می‌شوند.
- [x] وابستگی‌های Worker با روش سازگار با Hilt/Koin مدیریت می‌شوند.
- [x] نمونه‌های Singleton ضروری چندبار ساخته نمی‌شوند.
- [x] ViewModelها وابستگی‌های خود را از DI دریافت می‌کنند.

---

## 10) Paging 3 برای لیست‌های طولانی

- [x] برای نتایج طولانی جستجو از `Paging 3` استفاده شده است.
- [x] برای لیست آهنگ‌های پلی‌لیست از `Paging 3` استفاده شده است.
- [x] برای تاریخچه چت از `Paging 3` استفاده شده است.
- [x] کل داده‌های این لیست‌ها یک‌باره داخل `LazyColumn` بارگذاری نمی‌شوند.
- [x] `PagingSource` یا `RemoteMediator` متناسب با معماری پیاده‌سازی شده است.
- [x] Loading اولیه Paging نمایش داده می‌شود.
- [x] Loading انتهای لیست نمایش داده می‌شود.
- [x] Error اولیه Paging مدیریت می‌شود.
- [x] Error هنگام Append مدیریت می‌شود.
- [x] امکان Retry در خطای Paging وجود دارد.
- [x] Empty State لیست‌های Paging مدیریت می‌شود.
- [x] کلید پایدار یا `key` مناسب برای آیتم‌های Compose استفاده شده است.

---

## 11) Coroutines، I/O و Thread Safety

- [x] هیچ درخواست شبکه‌ای روی Main Thread اجرا نمی‌شود.
- [x] هیچ Query دیتابیس سنگینی روی Main Thread اجرا نمی‌شود.
- [~] عملیات فایل روی Main Thread اجرا نمی‌شود.
- [~] برای عملیات I/O از `Dispatchers.IO` یا abstraction مناسب استفاده شده است.
- [x] Coroutineها در Scope مناسب اجرا می‌شوند.
- [x] عملیات ViewModel در `viewModelScope` مدیریت می‌شود.
- [x] عملیات سرویس در Scope مناسب سرویس مدیریت می‌شود.
- [x] Jobهای طولانی با نابودی Component بی‌دلیل باقی نمی‌مانند.
- [x] Exceptionهای Coroutine مدیریت می‌شوند.
- [x] Flowها بدون Collectorهای تکراری و نشت حافظه استفاده می‌شوند.
- [x] داده‌های مشترک چند Thread به‌صورت ایمن مدیریت می‌شوند.

---

# بخش سوم: بک‌اند و داده‌های اولیه

## 12) انتخاب و پیاده‌سازی Backend

- [x] یک Backend واقعی برای پروژه وجود دارد.
- [x] نوع Backend مشخص است: `Ktor`، `PostgreSQL`، `Flyway` و `WebSocket`.
- [x] اتصال برنامه اندروید به Backend برقرار است.
- [x] خطاهای اتصال Backend مدیریت می‌شوند.
- [x] آدرس‌ها و کلیدهای محیطی به‌صورت امن و قابل تنظیم نگهداری می‌شوند.
- [x] داده‌ها فقط به Mock محلی محدود نشده‌اند.
- [x] Backend قابلیت ارائه لیست آهنگ‌ها را دارد.
- [x] Backend اطلاعات هنرمندان یا داده لازم برای جستجو را ارائه می‌کند.
- [x] Backend اطلاعات پلی‌لیست‌ها را ارائه می‌کند.
- [x] Backend اطلاعات کاربران و Follow را پشتیبانی می‌کند.
- [x] Backend یا سرویس Realtime بخش چت را پشتیبانی می‌کند.

---

## 13) حداقل 50 آهنگ واقعی

- [x] دیتابیس حداقل **50 آهنگ واقعی** دارد.
- [x] آهنگ‌ها تکراری و صرفاً رکورد آزمایشی یکسان نیستند.
- [x] فایل یا URL صدای هر آهنگ قابل پخش است.
- [x] تصویر کاور هر آهنگ قابل دریافت است.
- [x] داده‌های ناقص یا URLهای خراب بررسی شده‌اند.
- [~] آهنگ‌ها می‌توانند بی‌کلام، کوتاه یا از منبع مجاز انتخاب شوند.
- [x] تعداد داده‌ها برای نمایش Paging، Search و Sliderها کافی است.

> ممیزی داده در ۲۱ ژوئیهٔ ۲۰۲۶: تعداد ۱۶۷ فایل صوتی بررسی شد؛ همه با `ffprobe` مدت معتبر داشتند. چهار نسخه با محتوای باینری یکسان شناسایی شد و Seeder اکنون آن‌ها را با SHA-256 از رکوردهای دیتابیس حذف می‌کند؛ بنابراین ۱۶۳ ترک یکتای واقعی برای seed باقی می‌ماند. برای کاورهای فاقد تصویر نیز `covers/default.svg` اضافه شد.

---

## 14) متادیتای اجباری هر آهنگ

برای هر آهنگ حداقل فیلدهای زیر باید وجود داشته باشد:

- [x] `id`
- [x] `title`
- [x] `artist_name`
- [x] `cover_image_url`
- [x] `audio_url`

### کنترل کیفیت متادیتا

- [x] `id` هر آهنگ یکتا است.
- [x] `title` خالی نیست.
- [x] `artist_name` خالی نیست.
- [x] `cover_image_url` معتبر است.
- [x] `audio_url` معتبر و قابل استریم است.
- [~] UI در صورت خراب بودن کاور، Placeholder مناسب نشان می‌دهد.
- [x] UI در صورت خراب بودن فایل صوتی، خطای مناسب نشان می‌دهد.

---

# بخش چهارم: ساختار کلی رابط کاربری و انیمیشن‌ها

## 15) Bottom Navigation و پنج تب اصلی

- [x] Bottom Navigation در ساختار اصلی برنامه وجود دارد.
- [x] تب اول `Home` است.
- [x] تب دوم `Search` است.
- [x] تب سوم `Downloads` است.
- [x] تب چهارم `Playlists` است.
- [x] تب پنجم `Profile` است.
- [x] تب انتخاب‌شده به‌صورت واضح مشخص است.
- [x] با جابه‌جایی بین تب‌ها وضعیت ضروری هر صفحه حفظ می‌شود.
- [x] Bottom Navigation در فارسی و انگلیسی صحیح است.
- [x] مینی‌پلیر در صورت وجود آهنگ فعال، بالای Bottom Navigation قرار می‌گیرد.

---

## 16) Top Bar مشترک صفحات اصلی

در تمام صفحات اصلی:

- [x] سمت راست Top Bar لوگوی برنامه قرار دارد.
- [x] سمت راست Top Bar نام برنامه قرار دارد.
- [x] سمت چپ Top Bar عکس پروفایل کاربر قرار دارد.
- [x] سمت چپ Top Bar آیکون زنگوله اعلان‌ها قرار دارد.
- [x] سمت چپ Top Bar آیکون چرخ‌دنده تنظیمات قرار دارد.
- [x] جای عناصر در RTL و LTR درست تغییر می‌کند.
- [x] Top Bar در هر دو تم خوانا است.
- [x] کلیک روی آواتار رفتار مشخص دارد.
- [x] کلیک روی آیکون اعلان‌ها رفتار مشخص دارد.
- [x] کلیک روی آیکون تنظیمات، صفحه Settings را باز می‌کند.

> سند وجود آیکون‌ها را الزام کرده است؛ جزئیات صفحه اعلان‌ها را مشخص نکرده، اما آیکون نباید بدون رفتار رها شود.

---

## 17) انیمیشن‌ها و ریزتعامل‌ها

- [x] حرکت‌ها و Transitionهای کلی برنامه نرم هستند.
- [x] هنگام کلیک دکمه‌ها افکت Scale قابل مشاهده وجود دارد.
- [x] افکت کلیک باعث پرش یا تغییر نامطلوب چیدمان نمی‌شود.
- [x] باز و بسته شدن صفحات با Transition مناسب انجام می‌شود.
- [x] تغییر وضعیت Like انیمیشن مناسب دارد.
- [x] تغییر Play/Pause انیمیشن مناسب دارد.
- [x] نمایش و حذف آیتم‌ها نرم است.
- [x] انیمیشن‌ها باعث افت شدید فریم نمی‌شوند.
- [x] انیمیشن‌ها با Recomposition بی‌دلیل از ابتدا شروع نمی‌شوند.
- [x] در صورت فعال بودن کاهش انیمیشن سیستم، رفتار برنامه قابل قبول است.

---

## 18) Shimmer Loading

- [x] هنگام بارگذاری داده از سرور، Skeleton Loading نمایش داده می‌شود.
- [x] کارت‌های Home در حالت Loading، Shimmer دارند.
- [x] لیست‌های آهنگ در حالت Loading، Shimmer دارند.
- [x] کارت‌های پلی‌لیست در حالت Loading، Shimmer دارند.
- [x] نتایج Search در حالت Loading، Shimmer دارند.
- [x] بخش‌های پروفایل وابسته به سرور در حالت Loading، Shimmer دارند.
- [x] Shimmer پس از دریافت داده متوقف می‌شود.
- [x] Shimmer در تم روشن مناسب است.
- [x] Shimmer در تم تاریک مناسب است.
- [x] به‌جای نمایش صفحه خالی در زمان لود، Placeholder مناسب دیده می‌شود.

---

## 19) Shared Element Transition مینی‌پلیر به پلیر کامل

- [x] با کلیک روی Mini Player، صفحه Now Playing باز می‌شود.
- [x] کاور آهنگ از Mini Player به صفحه کامل با Shared Transition منتقل می‌شود.
- [x] کاور به‌صورت نرم بزرگ می‌شود.
- [x] کاور در صفحه کامل در مرکز قرار می‌گیرد.
- [x] Transition برگشت از صفحه کامل به Mini Player نیز طبیعی است.
- [x] Transition هنگام تغییر آهنگ خراب نمی‌شود.
- [x] در زمان انیمیشن، کاور Duplicate یا Flicker نمی‌زند.

---

# بخش پنجم: تب Home

## 20) Carousel پیشنهادات روز

- [x] در بالاترین بخش Home یک Carousel افقی وجود دارد.
- [x] Carousel تصاویر بزرگ دارد.
- [x] Carousel متحرک و روان است.
- [x] محتوای Carousel حداقل یکی از این موارد را نمایش می‌دهد: آلبوم جدید، پیشنهاد ویژه روز یا آهنگ ترند.
- [x] کلیک روی آیتم Carousel رفتار مشخص و کاربردی دارد.
- [x] تصاویر Carousel در حالت Loading، Shimmer دارند.
- [x] Carousel در RTL و LTR درست کار می‌کند.
- [x] متن روی تصاویر خوانا است.

---

## 21) Quick Actions خانه

چهار دسترسی سریع زیر باید وجود داشته باشد:

- [x] دکمه «آهنگ‌های لایک‌شده» وجود دارد.
- [x] دکمه «اخیراً شنیده‌شده» وجود دارد.
- [x] دکمه «پلی‌لیست‌های من» وجود دارد.
- [x] دکمه «هنرمندان برتر» وجود دارد.
- [x] هر چهار دکمه ظاهر یکپارچه دارند.
- [x] هر دکمه مقصد یا عملکرد درست دارد.
- [x] هنگام کلیک افکت Scale اجرا می‌شود.
- [x] متن و آیکون‌ها در هر دو زبان درست‌اند.

---

## 22) ردیف‌های افقی Home

- [x] ردیف «محبوب‌ترین‌ها» با `LazyRow` وجود دارد.
- [x] ردیف «جدیدترین‌ها» با `LazyRow` وجود دارد.
- [x] ردیف «پلی‌لیست‌های جهانی» با `LazyRow` وجود دارد.
- [x] ردیف «پلی‌لیست‌های محلی» با `LazyRow` وجود دارد.
- [x] آیتم‌های هر ردیف قابل کلیک‌اند.
- [x] انتخاب آهنگ باعث شروع پخش می‌شود.
- [x] انتخاب پلی‌لیست، صفحه جزئیات پلی‌لیست را باز می‌کند.
- [x] تصاویر در صورت خطا Placeholder دارند.
- [x] Loading هر ردیف Shimmer دارد.
- [x] عملکرد Scroll افقی روان است.

---

# بخش ششم: تب Search

## 23) جستجوی زنده با Debounce

- [x] ورودی جستجو وجود دارد.
- [x] Query جستجو در ViewModel مدیریت می‌شود.
- [x] با هر کاراکتر فوراً درخواست شبکه ارسال نمی‌شود.
- [x] از عملگر `debounce` در `Flow` استفاده شده است.
- [x] مدت Debounce مناسب و قابل قبول است.
- [x] Queryهای تکراری بی‌دلیل دوباره درخواست نمی‌شوند.
- [x] با خالی شدن Query، وضعیت صفحه درست Reset می‌شود.
- [x] درخواست قبلی در صورت تغییر سریع Query مدیریت یا لغو می‌شود.
- [x] Loading نتایج جستجو نمایش داده می‌شود.
- [x] خطای جستجو نمایش داده می‌شود.
- [x] نبود نتیجه با Empty State مناسب نمایش داده می‌شود.
- [x] نتایج طولانی با `Paging 3` دریافت می‌شوند.

---

## 24) فیلتر نتایج جستجو

- [x] چیپ فیلتر آهنگ وجود دارد.
- [x] چیپ فیلتر خواننده وجود دارد.
- [x] در صورت وجود نوع‌های بیشتر، چیپ‌های آن‌ها پیاده‌سازی شده‌اند.
- [x] فیلتر انتخاب‌شده مشخص است.
- [x] تغییر فیلتر نتیجه را به‌درستی به‌روزرسانی می‌کند.
- [x] فیلترها با Debounce و Paging هماهنگ هستند.
- [x] چیپ‌ها در RTL و LTR درست‌اند.
- [x] رنگ چیپ انتخاب‌شده در هر دو تم خوانا است.

---

## 25) تاریخچه جستجو

- [x] Queryهای قبلی در Room ذخیره می‌شوند.
- [x] تاریخچه در صفحه Search نمایش داده می‌شود.
- [x] امکان حذف یک مورد از تاریخچه وجود دارد.
- [x] حذف واقعاً از Room انجام می‌شود.
- [x] پس از بستن و بازکردن برنامه، تاریخچه باقی می‌ماند.
- [x] آیتم‌های تکراری به‌صورت منطقی مدیریت می‌شوند.
- [x] کلیک روی تاریخچه، جستجوی همان عبارت را اجرا می‌کند.
- [x] Empty State برای تاریخچه خالی وجود دارد.

---

# بخش هفتم: تب Downloads

## 26) نمایش دانلودها و پخش آفلاین

- [x] لیست آهنگ‌های دانلودشده نمایش داده می‌شود.
- [x] هر آیتم اطلاعات اصلی آهنگ را نشان می‌دهد.
- [x] فایل دانلودشده بدون اینترنت قابل پخش است.
- [x] آدرس فایل محلی در Room ذخیره شده است.
- [x] اگر فایل از حافظه حذف شده باشد، برنامه خطا را مدیریت می‌کند.
- [x] وضعیت دانلود ناقص یا خراب مشخص می‌شود.
- [x] Empty State برای نبود دانلود وجود دارد.

---

## 27) مرتب‌سازی دانلودها

- [x] امکان مرتب‌سازی لیست دانلودها وجود دارد.
- [x] روش یا گزینه‌های مرتب‌سازی در UI مشخص‌اند.
- [x] پس از مرتب‌سازی، لیست به‌درستی به‌روزرسانی می‌شود.
- [x] مرتب‌سازی با داده Room هماهنگ است.

> سند نوع دقیق مرتب‌سازی را مشخص نکرده است؛ حداقل یک روش کاربردی مانند نام، تاریخ دانلود یا هنرمند باید انتخاب شود.

---

## 28) حذف دانلود با Swipe to Dismiss

- [x] حذف با کشیدن آیتم پیاده‌سازی شده است.
- [x] انیمیشن Swipe روان است.
- [x] با حذف، رکورد Room حذف یا به‌روزرسانی می‌شود.
- [x] با حذف، فایل فیزیکی از حافظه مدیریت می‌شود.
- [x] در صورت نیاز امکان Undo یا تأیید حذف در نظر گرفته شده است.
- [x] حذف اشتباهی باعث Crash نمی‌شود.

---

# بخش هشتم: تب Playlists

## 29) گرید دو ستونه پلی‌لیست‌ها

- [x] از `LazyVerticalGrid` استفاده شده است.
- [x] گرید دو ستونه است.
- [x] کارت‌های پلی‌لیست رنگی هستند.
- [x] کارت‌ها تصویر، عنوان یا اطلاعات قابل فهم دارند.
- [x] کلیک روی کارت، جزئیات پلی‌لیست را باز می‌کند.
- [x] گرید در اندازه‌های مختلف صفحه به‌هم نمی‌ریزد.
- [x] Loading کارت‌ها Shimmer دارد.

---

## 30) بخش‌بندی پلی‌لیست‌ها

- [x] بخش «موسیقی جهان» وجود دارد.
- [x] بخش «موسیقی داخلی» وجود دارد.
- [x] بخش «پلی‌لیست‌های کاربر» وجود دارد.
- [x] عنوان هر بخش مشخص است.
- [x] داده هر بخش با بخش دیگر اشتباه نشده است.
- [x] Empty State هر بخش در صورت نبود داده مدیریت شده است.

---

## 31) صفحه جزئیات پلی‌لیست

این صفحه در سند ریزجزئیات کامل ندارد، اما برای اجرای الزامات زیر لازم است:

- [x] هدر یا کاور پلی‌لیست نمایش داده می‌شود.
- [x] عنوان و اطلاعات پلی‌لیست نمایش داده می‌شود.
- [x] لیست آهنگ‌های پلی‌لیست وجود دارد.
- [x] لیست طولانی آهنگ‌ها با `Paging 3` مدیریت می‌شود.
- [x] امکان پخش یک آهنگ وجود دارد.
- [x] امکان Play All یا Shuffle در طراحی در نظر گرفته شده است.
- [x] پلی‌لیست عمومی دوست قابل مشاهده و پخش است.
- [x] Empty State پلی‌لیست خالی وجود دارد.

---

# بخش نهم: تب Profile

## 32) آواتار کاربر

- [x] آواتار کاربر نمایش داده می‌شود.
- [x] امکان تغییر آواتار وجود دارد.
- [x] انتخاب یا دریافت تصویر به‌درستی مدیریت می‌شود.
- [x] آواتار جدید در UI به‌روز می‌شود.
- [x] آواتار در Backend یا محل ذخیره مناسب حفظ می‌شود.
- [x] Placeholder برای نبود آواتار وجود دارد.

---

## 33) وضعیت Premium

- [x] وضعیت عادی یا Premium کاربر مشخص است.
- [x] برای کاربر Premium نشانگر طلایی نمایش داده می‌شود.
- [x] Badge طلایی در تم روشن خوانا است.
- [x] Badge طلایی در تم تاریک خوانا است.
- [x] وضعیت Premium در `DataStore` ذخیره می‌شود.
- [x] پس از اجرای مجدد برنامه، وضعیت Premium باقی می‌ماند.

---

## 34) خرید یا تمدید نمایشی اشتراک

- [x] دکمه خرید یا تمدید اشتراک وجود دارد.
- [x] با کلیک روی دکمه، یک فرآیند نمایشی اجرا می‌شود.
- [x] پس از موفقیت فرآیند نمایشی، مقدار Premium برابر `true` می‌شود.
- [x] UI بلافاصله وضعیت Premium را نشان می‌دهد.
- [x] دکمه دانلود پس از Premium شدن فعال می‌شود.
- [x] وضعیت جدید در DataStore ذخیره می‌شود.

> پرداخت واقعی در سند الزام نشده است؛ یک فرایند نمایشی کافی است.

---

## 35) دسترسی به تنظیمات

- [x] از صفحه Profile دسترسی به Settings وجود دارد.
- [x] تغییر زبان از Settings ممکن است.
- [x] تغییر تم از Settings ممکن است.
- [x] تغییر اندازه فونت از Settings ممکن است.
- [x] خروج از حساب از Settings ممکن است.

---

# بخش دهم: مدیا پلیر حرفه‌ای

## 36) ExoPlayer و MediaSession

- [x] پخش موسیقی با `ExoPlayer` یا Media3 انجام می‌شود.
- [x] `MediaSession` پیاده‌سازی شده است.
- [x] عملیات پخش در یک `Background Service` انجام می‌شود.
- [x] خروج کاربر از صفحه پلیر باعث قطع موسیقی نمی‌شود.
- [x] خروج کاربر از برنامه باعث قطع موسیقی نمی‌شود.
- [x] کنترل‌های UI با وضعیت واقعی پلیر Sync هستند.
- [x] تغییر آهنگ از Notification در UI برنامه منعکس می‌شود.
- [x] وضعیت Play/Pause پس از بازگشت به برنامه درست است.
- [x] صف پخش یا آیتم بعدی به‌درستی مدیریت می‌شود.
- [x] منابع پلیر در زمان مناسب آزاد می‌شوند.

---

## 37) Audio Focus

- [] درخواست Audio Focus به‌درستی انجام می‌شود.
- [x] هنگام تماس ورودی، پخش متوقف یا Duck می‌شود.
- [x] هنگام پخش صدای سیستم یا برنامه دیگر، رفتار مناسب انجام می‌شود.
- [x] هنگام از دست‌دادن موقت Focus، وضعیت پلیر مدیریت می‌شود.
- [x] بعد از بازگشت Focus، پخش در صورت مناسب بودن ادامه پیدا می‌کند.
- [x] بعد از از دست‌دادن دائمی Focus، پخش خودکار بی‌دلیل شروع نمی‌شود.
- [x] حجم صدا پس از Duck به مقدار مناسب بازمی‌گردد.

---

## 38) کش هوشمند ExoPlayer

- [x] برای استریم از `CacheDataSource` استفاده شده است.
- [x] هنگام استریم، داده آهنگ همزمان در کش ذخیره می‌شود.
- [x] Replay آهنگ از کش استفاده می‌کند.
- [x] عقب و جلو کردن آهنگ تا حد ممکن از کش استفاده می‌کند.
- [x] کش باعث مصرف مجدد غیرضروری اینترنت نمی‌شود.
- [x] سیاست اندازه یا پاک‌سازی کش مشخص است.
- [x] خطای کش خراب مدیریت می‌شود.
- [x] کش موقت با دانلود آفلاین دائمی اشتباه گرفته نشده است.

---

## 39) Crossfade

- [x] در ثانیه‌های پایانی آهنگ، صدای آهنگ فعلی محو می‌شود.
- [x] در ثانیه‌های ابتدایی آهنگ بعدی، صدا افزایش می‌یابد.
- [x] دو آهنگ به‌صورت نرم با هم ترکیب می‌شوند.
- [x] Crossfade در رفتن دستی به آهنگ بعدی رفتار قابل قبول دارد.
- [x] Crossfade باعث دوبرابرشدن ناگهانی صدا نمی‌شود.
- [x] Crossfade هنگام نبود آهنگ بعدی خطا نمی‌دهد.

---

## 40) اعلان کنترل پخش و Lock Screen

- [x] وضعیت پخش در Notification سیستم نمایش داده می‌شود.
- [x] اطلاعات آهنگ جاری در Notification نمایش داده می‌شود.
- [x] کاور آهنگ در Notification نمایش داده می‌شود یا Placeholder مناسب دارد.
- [x] دکمه `Play/Pause` در Notification وجود دارد.
- [x] دکمه `Next` در Notification وجود دارد.
- [x] وضعیت پخش روی Lock Screen نمایش داده می‌شود.
- [x] کنترل‌های Lock Screen کار می‌کنند.
- [x] تغییر آهنگ از Notification با برنامه Sync است.
- [x] Notification Channel مناسب ساخته شده است.
- [x] اعلان در نسخه‌های مختلف اندروید با مجوز لازم مدیریت می‌شود.

---

## 41) Mini Player

- [x] Mini Player هنگام وجود آهنگ جاری نمایش داده می‌شود.
- [x] Mini Player بالای Bottom Navigation شناور است.
- [x] نام آهنگ جاری را نشان می‌دهد.
- [x] نام هنرمند یا اطلاعات مکمل را نشان می‌دهد.
- [x] کاور آهنگ را نشان می‌دهد.
- [x] کنترل Play/Pause دارد.
- [x] وضعیت Mini Player با پلیر واقعی Sync است.
- [x] کلیک روی Mini Player، Now Playing را باز می‌کند.
- [x] Mini Player روی محتوای صفحه قرار نمی‌گیرد یا فضای لازم رعایت شده است.
- [x] Shared Element Transition از کاور Mini Player اجرا می‌شود.

---

## 42) صفحه Now Playing

- [x] صفحه کامل پخش فعلی وجود دارد.
- [x] اطلاعات آهنگ جاری نمایش داده می‌شود.
- [x] عنوان آهنگ نمایش داده می‌شود.
- [x] نام خواننده نمایش داده می‌شود.
- [x] کاور آهنگ در مرکز صفحه نمایش داده می‌شود.
- [x] کنترل Play/Pause وجود دارد.
- [x] کنترل Next وجود دارد.
- [x] Seek یا نوار زمان پخش به‌درستی کار می‌کند.
- [x] زمان جاری و مدت کل آهنگ درست نمایش داده می‌شوند.
- [x] دکمه Like یا Download در محل مناسب در صورت طراحی وجود دارد.
- [x] صفحه با Mini Player و MediaSession Sync است.

---

## 43) کاور چرخان شبیه CD

- [x] کاور به‌شکل دیسک یا CD طراحی شده است.
- [x] هنگام پخش، کاور می‌چرخد.
- [x] هنگام Pause، چرخش متوقف می‌شود.
- [x] بعد از Resume، چرخش به‌صورت طبیعی ادامه پیدا می‌کند.
- [x] با تغییر آهنگ، تصویر کاور عوض می‌شود.
- [x] انیمیشن چرخش باعث مصرف غیرعادی منابع نمی‌شود.

---

## 44) رنگ‌بندی پویا با Palette API

- [x] رنگ پس‌زمینه صفحه پلیر ثابت نیست.
- [x] رنگ غالب کاور آهنگ استخراج می‌شود.
- [x] رنگ استخراج‌شده در پس‌زمینه اعمال می‌شود.
- [x] پس‌زمینه به‌صورت گرادیانت نرم طراحی شده است.
- [x] با تغییر آهنگ، رنگ پس‌زمینه تغییر می‌کند.
- [x] تغییر رنگ با Transition نرم انجام می‌شود.
- [x] در صورت خطای تصویر، رنگ پیش‌فرض مناسب وجود دارد.
- [x] خوانایی متن روی رنگ‌های روشن و تیره حفظ می‌شود.

---

## 45) Audio Visualizer با Canvas

- [x] Visualizer صوتی در صفحه پلیر وجود دارد.
- [x] Visualizer به‌شکل Equalizer یا Waveform طراحی شده است.
- [x] Visualizer با `Canvas` در Compose رسم شده است.
- [x] برای Visualizer از `Lottie` استفاده نشده است.
- [x] برای Visualizer از `GIF` استفاده نشده است.
- [x] هنگام پخش، Visualizer حرکت می‌کند.
- [x] هنگام Pause، رفتار Visualizer متوقف یا متناسب می‌شود.
- [x] انیمیشن Visualizer روان است.
- [x] Visualizer باعث افت محسوس عملکرد نمی‌شود.

---

## 46) Sleep Timer

- [x] کاربر می‌تواند Sleep Timer تنظیم کند.
- [x] حداقل یک زمان نمونه مانند 15 دقیقه وجود دارد.
- [x] شمارش زمان با `Coroutines` انجام می‌شود.
- [x] پس از پایان زمان، پخش خودکار متوقف می‌شود.
- [x] کاربر می‌تواند تایمر را لغو کند.
- [x] وضعیت تایمر فعال در UI مشخص است.
- [x] با تغییر صفحه، تایمر بی‌دلیل از بین نمی‌رود.
- [x] اجرای چند تایمر همزمان جلوگیری یا مدیریت می‌شود.

---

## 47) کنترل سرعت پخش

- [x] امکان انتخاب سرعت `1.5x` وجود دارد.
- [x] امکان انتخاب سرعت `2x` وجود دارد.
- [x] سرعت عادی `1x` نیز قابل بازگشت است.
- [x] سرعت از طریق کنترلرهای ExoPlayer تغییر می‌کند.
- [x] سرعت انتخاب‌شده در UI مشخص است.
- [x] تغییر آهنگ رفتار سرعت را مطابق تصمیم طراحی حفظ یا Reset می‌کند.
- [x] سرعت واقعی صدا با مقدار UI یکسان است.

---

# بخش یازدهم: Business Logic

## 48) تفاوت کاربر عادی و Premium

- [x] کاربر عادی می‌تواند همه آهنگ‌ها را کامل گوش دهد.
- [x] کاربر عادی محدودیت زمانی پخش ندارد.
- [x] برای کاربر عادی آهنگ پس از چند ثانیه قطع نمی‌شود.
- [x] تفاوت اصلی کاربر عادی و Premium فقط دانلود آفلاین است.
- [x] دکمه دانلود فقط برای کاربر Premium فعال است یا رفتار محدودکننده مشخص دارد.
- [x] اگر کاربر عادی روی Download بزند، پیام نیاز به ارتقای حساب نمایش داده می‌شود.
- [x] پیام ارتقا از منابع متنی خوانده می‌شود.
- [x] پس از Premium شدن، دانلود بدون Restart فعال می‌شود.

> نکته بسیار مهم: طبق سند، کاربر عادی حق پخش کامل و نامحدود دارد؛ نباید محدودیت پخش شبیه Spotify رایگان ایجاد شود.

---

## 49) دانلود با WorkManager

- [x] دانلود فقط برای کاربر Premium شروع می‌شود.
- [x] دانلود آهنگ به `WorkManager` سپرده می‌شود.
- [x] دانلود در پس‌زمینه ادامه پیدا می‌کند.
- [x] با خروج از صفحه دانلود قطع نمی‌شود.
- [x] وضعیت دانلود قابل مشاهده است.
- [x] Progress دانلود در صورت طراحی نمایش داده می‌شود.
- [x] موفقیت دانلود ثبت می‌شود.
- [x] خطای دانلود مدیریت می‌شود.
- [x] امکان Retry برای دانلود ناموفق وجود دارد.
- [x] فایل در مسیر مناسب ذخیره می‌شود.
- [x] آدرس فایل دانلودشده در Room ذخیره می‌شود.
- [x] رکورد تکراری دانلود مدیریت می‌شود.
- [x] محدودیت شبکه یا Constraint مورد نیاز مشخص شده است.

---

## 50) پخش هوشمند Local-First

- [x] هنگام Play، Repository وجود نسخه آفلاین را بررسی می‌کند.
- [x] اگر فایل محلی معتبر باشد، از فایل محلی پخش می‌شود.
- [x] اگر فایل محلی موجود نباشد، از URL شبکه استریم می‌شود.
- [x] بررسی فایل محلی در UI انجام نمی‌شود و در Repository یا لایه مناسب است.
- [x] در حالت Airplane Mode، فایل دانلودشده قابل پخش است.
- [x] اگر رکورد Room وجود داشته ولی فایل فیزیکی حذف شده باشد، برنامه به‌درستی fallback یا خطا می‌دهد.
- [x] پخش فایل محلی مصرف اینترنت ندارد.

---

# بخش دوازدهم: شبکه اجتماعی و چت Realtime

## 51) جستجو و Follow کاربران

- [x] کاربران می‌توانند پروفایل کاربران دیگر را جستجو کنند.
- [x] نتایج جستجوی کاربران نمایش داده می‌شود.
- [x] پروفایل کاربر دیگر قابل مشاهده است.
- [x] دکمه Follow وجود دارد.
- [x] با Follow، وضعیت UI به‌روز می‌شود.
- [x] رابطه Follow در Backend ذخیره می‌شود.
- [x] دکمه Unfollow برای کاربر دنبال‌شده وجود دارد.
- [x] با Unfollow، وضعیت UI و Backend به‌روز می‌شود.
- [x] لیست کاربران دنبال‌شده قابل مشاهده است.

---

## 52) پلی‌لیست‌های عمومی دوستان

- [x] کاربر می‌تواند پلی‌لیست‌های Public دوستان را ببیند.
- [x] پلی‌لیست Private به‌عنوان Public نمایش داده نمی‌شود.
- [x] جزئیات پلی‌لیست عمومی باز می‌شود.
- [x] آهنگ‌های پلی‌لیست عمومی قابل مشاهده‌اند.
- [x] آهنگ‌های پلی‌لیست عمومی قابل پخش‌اند.
- [x] لیست طولانی آهنگ‌ها با Paging مدیریت می‌شود.
- [x] Empty State برای نداشتن پلی‌لیست عمومی وجود دارد.

---

## 53) WebSocket و Realtime DM

- [x] دایرکت مسیج وجود دارد.
- [x] اتصال چت با `WebSocket` انجام می‌شود.
- [x] پیام جدید بدون Refresh دستی نمایش داده می‌شود.
- [x] لیست پیام‌ها به‌صورت زنده به‌روزرسانی می‌شود.
- [x] از Polling برای دریافت پیام‌ها استفاده نشده است.
- [x] قطع اتصال WebSocket مدیریت می‌شود.
- [x] اتصال مجدد پس از برگشت اینترنت مدیریت می‌شود.
- [x] پیام‌های تکراری پس از Reconnect کنترل می‌شوند.
- [x] ترتیب زمانی پیام‌ها درست است.
- [x] تاریخچه طولانی پیام‌ها با `Paging 3` مدیریت می‌شود.
- [x] ارسال و دریافت پیام با Flow یا جریان داده مناسب به UI منتقل می‌شود.

> استفاده از Polling طبق سند به هیچ عنوان پذیرفته نیست.

---

## 54) وضعیت پیام‌ها

برای پیام ارسالی باید این حالت‌ها وجود داشته باشد:

- [x] حالت «در حال ارسال» با آیکون ساعت.
- [x] حالت «ارسال شده» با تک‌تیک.
- [x] حالت «خوانده شده» با دو تیک.
- [x] تغییر وضعیت‌ها بدون ایجاد پیام جدید در UI انجام می‌شود.
- [x] وضعیت پیام در Room یا Backend به‌درستی ثبت می‌شود.
- [x] در ارسال ناموفق، حالت خطا یا Retry مدیریت شده است.
- [x] وضعیت پیام بعد از بازکردن مجدد چت درست باقی می‌ماند.

---

## 55) Typing Indicator

- [x] هنگام تایپ کاربر مقابل، عبارت `is typing...` یا ترجمه آن نمایش داده می‌شود.
- [x] Typing Indicator به‌صورت Realtime دریافت می‌شود.
- [x] پس از توقف تایپ، Indicator پنهان می‌شود.
- [x] Indicator برای کاربر اشتباه نمایش داده نمی‌شود.
- [x] متن Typing در فارسی و انگلیسی موجود است.
- [x] ارسال رویداد Typing بیش‌ازحد و بدون محدودسازی انجام نمی‌شود.

---

## 56) اشتراک‌گذاری آهنگ در دایرکت

- [x] روی آهنگ دکمه Share وجود دارد.
- [x] کاربر می‌تواند دوست مقصد را انتخاب کند.
- [x] آهنگ به‌صورت پیام چت ارسال می‌شود.
- [x] پیام آهنگ UI سفارشی دارد.
- [x] پیام آهنگ به‌شکل Mini Card نمایش داده می‌شود.
- [x] Mini Card حداقل اطلاعات قابل شناسایی آهنگ را نشان می‌دهد.
- [x] با کلیک روی Mini Card، آهنگ مستقیماً در پلیر پخش می‌شود.
- [x] وضعیت پیام آهنگ نیز Sending/Sent/Read را دارد.
- [x] پیام آهنگ در حالت آفلاین از تاریخچه قابل مشاهده است.

---

## 57) تاریخچه آفلاین چت

- [x] پیام‌های دریافت‌شده در Room ذخیره می‌شوند.
- [x] پیام‌های ارسال‌شده مورد نیاز نیز برای نمایش تاریخچه ذخیره می‌شوند.
- [x] هنگام قطع اینترنت، تاریخچه چت قابل خواندن است.
- [x] ترتیب پیام‌های آفلاین درست است.
- [x] پس از اتصال مجدد، داده محلی و سرور همگام می‌شوند.
- [x] پیام‌ها بی‌دلیل Duplicate نمی‌شوند.
- [x] تاریخچه طولانی با Paging از Room خوانده می‌شود.
- [x] Empty State برای چت بدون پیام وجود دارد.

---

# بخش سیزدهم: مدیریت داده‌های محلی

## 58) Room Database

مواردی که طبق سند باید در Room ذخیره شوند:

- [x] تاریخچه جستجو.
- [x] لیست آهنگ‌های لایک‌شده.
- [x] آدرس فایل‌های دانلودشده.
- [x] تاریخچه پیام‌های آفلاین.

### کیفیت پیاده‌سازی Room

- [x] Entityهای لازم تعریف شده‌اند.
- [x] DAOهای لازم تعریف شده‌اند.
- [x] Queryها به‌صورت `suspend` یا `Flow` مناسب هستند.
- [x] نسخه دیتابیس مشخص است.
- [x] Migration یا راهبرد نسخه‌بندی دیتابیس در نظر گرفته شده است.
- [x] Queryها روی Main Thread اجرا نمی‌شوند.
- [x] کلیدهای اصلی و Indexهای لازم منطقی‌اند.
- [x] داده تکراری مدیریت می‌شود.
- [x] حذف و به‌روزرسانی داده‌ها UI را Sync می‌کند.

---

## 59) DataStore Preferences

مواردی که طبق سند باید با DataStore مدیریت شوند:

- [x] حالت Dark Mode یا انتخاب تم.
- [x] اندازه فونت.
- [x] زبان انتخابی.
- [x] وضعیت کاربر عادی یا Premium.

### کیفیت پیاده‌سازی DataStore

- [x] از SharedPreferences سنتی به‌جای DataStore برای این موارد استفاده نشده است.
- [x] مقادیر DataStore به‌صورت Flow خوانده می‌شوند.
- [x] تغییر مقدارها در Coroutine مناسب انجام می‌شود.
- [x] مقدار پیش‌فرض هر تنظیم مشخص است.
- [x] خراب یا خالی بودن مقدارها مدیریت می‌شود.
- [x] تنظیمات پس از Restart حفظ می‌شوند.

---

# بخش چهاردهم: صفحات فرعی و UX تکمیلی

## 60) صفحه آهنگ‌های لایک‌شده

- [x] صفحه جداگانه برای آهنگ‌های لایک‌شده وجود دارد.
- [x] صفحه فقط یک لیست ساده و خام نیست.
- [x] هدر جذاب دارد.
- [x] دکمه `Play All` یا `Shuffle` دارد.
- [x] امکان حذف سریع آهنگ از لیست وجود دارد.
- [x] حذف با انیمیشن مانند Swipe to Dismiss پیاده‌سازی شده است.
- [x] لیست لایک‌ها از Room خوانده می‌شود.
- [x] تغییر Like در سایر صفحات با این صفحه Sync است.
- [x] Empty State برای نبود آهنگ لایک‌شده وجود دارد.

---

## 61) صفحه اخیراً شنیده‌شده

- [x] صفحه جداگانه برای Recently Played وجود دارد.
- [x] صفحه فقط یک لیست ساده و خام نیست.
- [x] هدر جذاب دارد.
- [x] دکمه `Play All` یا `Shuffle` دارد.
- [x] امکان حذف سریع آیتم از لیست وجود دارد.
- [x] حذف با انیمیشن مناسب انجام می‌شود.
- [x] ترتیب پخش‌های اخیر منطقی است.
- [x] Empty State برای نبود تاریخچه وجود دارد.

---

## 62) لیست هنرمندان و کاربران دنبال‌شده

- [x] صفحه یا بخش نمایش هنرمندان دنبال‌شده وجود دارد.
- [x] صفحه یا بخش نمایش کاربران دنبال‌شده وجود دارد.
- [x] رابط کاربری به‌شکل Grid یا List تمیز است.
- [x] آواتار یا تصویر هر مورد نمایش داده می‌شود.
- [x] نام هر مورد نمایش داده می‌شود.
- [x] دکمه Unfollow وجود دارد.
- [x] بعد از Unfollow، آیتم و وضعیت به‌درستی به‌روز می‌شود.
- [x] Empty State برای لیست خالی وجود دارد.

---

## 63) صفحه Settings

- [x] صفحه Settings جداگانه و استاندارد وجود دارد.
- [x] گزینه زبان فارسی وجود دارد.
- [x] گزینه زبان انگلیسی وجود دارد.
- [x] تغییر زبان به‌صورت داینامیک RTL/LTR را تغییر می‌دهد.
- [x] گزینه تم روشن وجود دارد.
- [x] گزینه تم تاریک وجود دارد.
- [x] گزینه تم سیستم وجود دارد.
- [x] گزینه تغییر اندازه فونت وجود دارد.
- [x] گزینه خروج از حساب کاربری وجود دارد.
- [x] مقادیر انتخابی فعلی در UI مشخص‌اند.
- [x] تنظیمات در DataStore ذخیره می‌شوند.
- [x] Settings در هر دو زبان و تم درست نمایش داده می‌شود.

---

## 64) Empty States

- [x] برای پلی‌لیست خالی Empty State طراحی شده است.
- [x] برای جستجوی بدون نتیجه Empty State طراحی شده است.
- [x] برای قطع اینترنت Empty State طراحی شده است.
- [x] برای دانلودهای خالی Empty State طراحی شده است.
- [x] برای آهنگ‌های لایک‌شده خالی Empty State طراحی شده است.
- [x] برای چت بدون پیام Empty State طراحی شده است.
- [x] Empty Stateها تصویر یا انیمیشن مناسب دارند.
- [x] Empty Stateها متن واضح و کاربردی دارند.
- [x] در صورت امکان CTA مناسب مانند Retry یا Start Exploring دارند.
- [x] Empty Stateها با دیزاین سیستم هماهنگ‌اند.

---

# بخش پانزدهم: نکات تکمیلی که سند جزئیاتشان را آزاد گذاشته است

> موارد این بخش برای کامل‌کردن جریان‌های پروژه ضروری یا بسیار منطقی‌اند، اما جزئیات دقیق آن‌ها در سند تعیین نشده است. هنگام مقایسه، بررسی شود که برای هرکدام تصمیم منسجم گرفته شده باشد.

## 65) ناوبری و صفحات لازم برای تکمیل Flowها

- [x] مسیر Navigation پنج تب اصلی تعریف شده است.
- [x] مسیر صفحه Now Playing تعریف شده است.
- [x] مسیر جزئیات پلی‌لیست تعریف شده است.
- [x] مسیر پروفایل سایر کاربران تعریف شده است.
- [x] مسیر لیست Followها تعریف شده است.
- [x] مسیر لیست چت‌ها تعریف شده است.
- [x] مسیر صفحه DM تعریف شده است.
- [x] مسیر Settings تعریف شده است.
- [x] مسیر Liked Songs تعریف شده است.
- [x] مسیر Recently Played تعریف شده است.
- [x] مسیر اعلان‌ها یا رفتار جایگزین آیکون اعلان مشخص است.
- [x] Back Stack صفحات به‌درستی مدیریت می‌شود.
- [x] Deep Link یا بازشدن مقصد پیام آهنگ در صورت پیاده‌سازی درست است.

---

## 66) وضعیت‌های عمومی هر صفحه

برای هر صفحه وابسته به داده بررسی شود:

- [x] Loading State دارد.
- [x] Content/Success State دارد.
- [x] Empty State دارد.
- [x] Error State دارد.
- [x] Retry Action دارد.
- [~] وضعیت قطع اینترنت را مدیریت می‌کند.
- [x] Recomposition باعث درخواست شبکه تکراری نمی‌شود.
- [x] داده قبلی در زمان Refresh بی‌دلیل حذف نمی‌شود.

> مدیریت آفلاین برای Search، Downloads، Auth و Chat صریح است؛ برخی صفحات کاتالوگ هنوز قطع اینترنت را با پیام عمومی خطای بارگذاری نمایش می‌دهند.

---

## 67) مدل‌های داده مورد نیاز برای جریان‌های پروژه

حداقل مدل یا ساختار معادل برای این مفاهیم وجود دارد:

- [x] `Song`
- [x] `Artist`
- [x] `User`
- [x] `Playlist`
- [x] `PlaylistSong`
- [x] `Follow`
- [x] `Chat` یا `Conversation`
- [x] `Message`
- [x] `SharedSongMessage`
- [x] `SearchHistory`
- [x] `LikedSong`
- [x] `DownloadedSong`
- [x] وضعیت اشتراک کاربر
- [x] وضعیت پیام
- [x] وضعیت دانلود

> فقط پنج فیلد Song در سند اجباری و دقیق ذکر شده‌اند؛ فیلدهای دیگر باید بر اساس طراحی Backend تعیین شوند.

---

## 68) مدیریت خطا و شبکه

- [~] خطای نبود اینترنت پیام مناسب دارد.
- [x] خطای Timeout مدیریت می‌شود.
- [x] خطای HTTP یا Backend مدیریت می‌شود.
- [x] خطای URL خراب آهنگ مدیریت می‌شود.
- [x] خطای تصویر کاور مدیریت می‌شود.
- [x] خطای دانلود مدیریت می‌شود.
- [x] خطای WebSocket مدیریت می‌شود.
- [x] قطع و وصل اینترنت باعث Crash نمی‌شود.
- [x] Retry در نقاط مناسب وجود دارد.
- [x] پیام خطا یک‌بار نمایش داده می‌شود و در Loop نمی‌افتد.

---

## 69) کارایی و پایداری Compose

- [x] لیست‌ها از `LazyColumn`، `LazyRow` یا `LazyVerticalGrid` مناسب استفاده می‌کنند.
- [x] برای آیتم‌ها `key` پایدار وجود دارد.
- [x] Stateهای محاسباتی با روش مناسب Memoize شده‌اند.
- [~] انیمیشن‌ها Recomposition اضافی ایجاد نمی‌کنند.
- [x] تصاویر با ابعاد مناسب بارگذاری می‌شوند.
- [~] اسکرول صفحات اصلی روان است.
- [x] باز و بسته‌کردن مکرر پلیر باعث نشت منابع نمی‌شود.
- [x] سرویس پخش و WebSocket Lifecycle مشخص دارند.
- [x] دانلودهای WorkManager بی‌دلیل چندبار Enqueue نمی‌شوند.

> چرخهٔ عمر WebSocket با `ON_START/ON_STOP` هماهنگ و مالکیت کنترلر singleton پلیر از MediaSession سرویس جدا شد. روانی اسکرول و هزینهٔ Recomposition انیمیشن‌ها برای تأیید نهایی به تست پروفایل روی دستگاه واقعی نیاز دارند.

---

# بخش شانزدهم: سناریوهای تست پذیرش

## 70) تست طراحی و Localization

- [ ] برنامه را با زبان فارسی اجرا کردم و همه صفحات RTL بودند.
- [ ] برنامه را با زبان انگلیسی اجرا کردم و همه صفحات LTR بودند.
- [ ] هیچ متن مستقیم و ترجمه‌نشده‌ای در UI ندیدم.
- [ ] تم روشن را در تمام صفحات اصلی تست کردم.
- [ ] تم تاریک را در تمام صفحات اصلی تست کردم.
- [ ] حالت System Theme را تست کردم.
- [ ] تغییر زبان و تم پس از Restart حفظ شد.
- [ ] اندازه فونت تغییر کرد و پس از Restart حفظ شد.

---

## 71) تست معماری و State

- [ ] هر صفحه UiState مشخص دارد.
- [ ] State هر صفحه از StateFlow می‌آید.
- [ ] Eventهای کاربر به ViewModel ارسال می‌شوند.
- [ ] Snackbar بعد از Rotation/Recomposition تکراری نمایش داده نمی‌شود.
- [ ] هیچ فراخوانی مستقیم API داخل Composable وجود ندارد.
- [ ] هیچ Query مستقیم Room داخل Composable وجود ندارد.
- [ ] Paging جستجو، پلی‌لیست و چت در عمل مرحله‌ای بارگذاری می‌شود.

---

## 72) تست Home و Navigation

- [ ] Carousel Home داده واقعی نشان می‌دهد.
- [ ] هر چهار Quick Action کار می‌کنند.
- [ ] هر چهار LazyRow موردنیاز وجود دارند.
- [ ] تمام پنج تب Bottom Navigation کار می‌کنند.
- [ ] Top Bar در تمام صفحات اصلی کامل است.
- [ ] Settings از Top Bar و Profile باز می‌شود.
- [ ] Mini Player با تغییر تب باقی می‌ماند.

---

## 73) تست Search

- [ ] با تایپ سریع، برای هر حرف Request جدا ارسال نمی‌شود.
- [ ] بعد از Debounce نتیجه نمایش داده می‌شود.
- [ ] فیلتر آهنگ کار می‌کند.
- [ ] فیلتر خواننده کار می‌کند.
- [ ] Query در Room ذخیره می‌شود.
- [ ] حذف تاریخچه واقعاً پایدار است.
- [ ] جستجوی بدون نتیجه Empty State دارد.
- [ ] نتایج زیاد با Paging لود می‌شوند.

---

## 74) تست Premium و Download

- [ ] با کاربر عادی همه آهنگ‌ها کامل پخش می‌شوند.
- [ ] کاربر عادی با Download پیام ارتقا می‌بیند.
- [ ] با خرید نمایشی، Premium برابر true می‌شود.
- [ ] Badge طلایی ظاهر می‌شود.
- [ ] دانلود برای Premium فعال می‌شود.
- [ ] دانلود توسط WorkManager در پس‌زمینه ادامه پیدا می‌کند.
- [ ] فایل دانلودشده بدون اینترنت پخش می‌شود.
- [ ] Repository نسخه محلی را به URL اینترنت ترجیح می‌دهد.
- [ ] حذف با Swipe فایل و رکورد را مدیریت می‌کند.

---

## 75) تست Player

- [ ] با خروج از برنامه موسیقی ادامه پیدا می‌کند.
- [ ] Notification کنترل پخش نمایش داده می‌شود.
- [ ] Play/Pause از Notification کار می‌کند.
- [ ] Next از Notification کار می‌کند.
- [ ] کنترل Lock Screen کار می‌کند.
- [ ] تماس یا صدای برنامه دیگر Audio Focus را فعال می‌کند.
- [ ] پس از برگشت Focus، پخش رفتار درست دارد.
- [ ] Replay یا Seek از Cache استفاده می‌کند.
- [ ] Crossfade بین دو آهنگ شنیده می‌شود.
- [ ] Mini Player اطلاعات درست نشان می‌دهد.
- [ ] Shared Transition کاور درست اجرا می‌شود.
- [ ] کاور هنگام Play می‌چرخد.
- [ ] کاور هنگام Pause می‌ایستد.
- [ ] رنگ پس‌زمینه با کاور تغییر می‌کند.
- [ ] Visualizer با Canvas حرکت می‌کند.
- [ ] Sleep Timer پخش را در زمان تعیین‌شده متوقف می‌کند.
- [ ] سرعت 1.5x و 2x واقعاً اعمال می‌شود.

---

## 76) تست Realtime Chat

- [ ] دو کاربر آزمایشی برای چت وجود دارند.
- [ ] پیام جدید بدون Refresh نمایش داده می‌شود.
- [ ] در Network Inspector یا کد، Polling استفاده نشده است.
- [ ] حالت ساعت هنگام Sending دیده می‌شود.
- [ ] تک‌تیک پس از ارسال دیده می‌شود.
- [ ] دو تیک پس از خواندن دیده می‌شود.
- [ ] Typing Indicator زنده کار می‌کند.
- [ ] قطع WebSocket و اتصال مجدد تست شده است.
- [ ] پیام تکراری بعد از Reconnect ایجاد نمی‌شود.
- [ ] آهنگ به دایرکت Share می‌شود.
- [ ] Mini Card آهنگ در چت نمایش داده می‌شود.
- [ ] کلیک روی Mini Card آهنگ را پخش می‌کند.
- [ ] با قطع اینترنت تاریخچه Room قابل خواندن است.
- [ ] تاریخچه طولانی با Paging بارگذاری می‌شود.

---

# بخش هفدهم: خروجی نهایی تحویل

## 77) فایل اپلیکیشن

- [ ] فایل خروجی نصب‌شدنی اپلیکیشن آماده شده است.
- [ ] فایل روی گوشی واقعی یا Emulator نصب و اجرا شده است.
- [ ] نسخه Release یا خروجی مناسب تحویل ساخته شده است.
- [ ] نام فایل خروجی واضح است.
- [ ] برنامه پس از نصب اولیه بدون Crash اجرا می‌شود.
- [ ] Backend مورد نیاز در زمان بررسی در دسترس است.

> طبق سند، نیازی به آپلود Source Code پروژه نیست.

---

## 78) ویدئوی کوتاه عملکرد برنامه

- [ ] ویدئوی کوتاه از عملکرد برنامه ضبط شده است.
- [ ] Home و پنج تب اصلی در ویدئو دیده می‌شوند.
- [ ] تغییر زبان فارسی/انگلیسی نشان داده می‌شود.
- [ ] تغییر تم نشان داده می‌شود.
- [ ] جستجو و Debounce به‌صورت قابل فهم نمایش داده می‌شود.
- [ ] پخش موسیقی و Mini Player نمایش داده می‌شود.
- [ ] صفحه Now Playing و انیمیشن‌ها نمایش داده می‌شوند.
- [ ] Notification یا پخش پس‌زمینه نمایش داده می‌شود.
- [ ] Premium و دانلود آفلاین نمایش داده می‌شود.
- [ ] چت Realtime و وضعیت پیام‌ها نمایش داده می‌شود.
- [ ] Share آهنگ داخل چت نمایش داده می‌شود.
- [ ] ویدئو بدون توقف‌های طولانی و بخش‌های اضافی است.
- [ ] کیفیت تصویر و خوانایی متن‌های ویدئو مناسب است.

---

# بخش هجدهم: خط قرمزهای نهایی قبل از تحویل

هرکدام از موارد زیر اگر وجود داشته باشد، باید قبل از تحویل اصلاح شود:

- [ ] هیچ رنگ، فونت یا فاصله مهمی به‌صورت Hardcode پراکنده باقی نمانده است.
- [ ] هیچ متن قابل مشاهده‌ای مستقیم داخل کد باقی نمانده است.
- [ ] هیچ صفحه‌ای فقط فارسی یا فقط انگلیسی نیست.
- [ ] RTL و LTR در تمام صفحات درست‌اند.
- [ ] تم تاریک در هیچ صفحه‌ای متن ناخوانا ندارد.
- [ ] آیکون برنامه پیش‌فرض اندروید نیست.
- [ ] معماری Data/Domain/UI رعایت شده است.
- [ ] UiState و StateFlow برای صفحات رعایت شده‌اند.
- [ ] One-time Eventها با SharedFlow یا Channel مدیریت شده‌اند.
- [ ] DI با Hilt یا Koin وجود دارد.
- [ ] Search، Playlist Songs و Chat History از Paging 3 استفاده می‌کنند.
- [ ] هیچ Network/Database I/O روی Main Thread نیست.
- [ ] حداقل 50 آهنگ واقعی و قابل پخش وجود دارد.
- [ ] Shimmer برای Loading لیست‌ها و کارت‌ها وجود دارد.
- [ ] Mini Player به Now Playing با Shared Transition وصل است.
- [ ] پخش پس‌زمینه با Service و MediaSession کار می‌کند.
- [ ] Audio Focus کار می‌کند.
- [ ] CacheDataSource استفاده شده است.
- [ ] Crossfade وجود دارد.
- [ ] Notification و Lock Screen Control کار می‌کنند.
- [ ] Visualizer با Canvas نوشته شده و Lottie/GIF نیست.
- [ ] Sleep Timer با Coroutine کار می‌کند.
- [ ] سرعت 1.5x و 2x کار می‌کند.
- [ ] کاربر عادی محدودیت پخش ندارد.
- [ ] فقط دانلود آفلاین برای Premium است.
- [ ] دانلود با WorkManager انجام می‌شود.
- [ ] فایل محلی قبل از URL شبکه بررسی می‌شود.
- [ ] چت با WebSocket است و Polling نیست.
- [ ] Sending/Sent/Read نمایش داده می‌شود.
- [ ] Typing Indicator وجود دارد.
- [ ] Share آهنگ با Mini Card و پخش مستقیم کار می‌کند.
- [ ] تاریخچه چت آفلاین در Room است.
- [ ] Room چهار دسته داده الزامی را نگه می‌دارد.
- [ ] DataStore چهار تنظیم الزامی را نگه می‌دارد.
- [ ] صفحات Liked، Recent، Followed و Settings کامل‌اند.
- [ ] Empty Stateهای اصلی طراحی شده‌اند.
- [ ] APK یا فایل خروجی نصب‌شدنی آماده است.
- [ ] ویدئوی کوتاه عملکرد آماده است.

---

# جمع‌بندی وضعیت پروژه

## آمار دستی

- تعداد موارد کامل: `...`
- تعداد موارد ناقص: `...`
- تعداد موارد شروع‌نشده: `...`
- مهم‌ترین بخش‌های باقی‌مانده: `...`
- خطاهای بحرانی قبل از تحویل: `...`

## اولویت پیشنهادی رفع نواقص

1. الزامات صریح معماری و استانداردها.
2. مدیا پلیر پس‌زمینه، MediaSession و Audio Focus.
3. WebSocket و چت Realtime.
4. Paging 3، Room، DataStore و WorkManager.
5. منطق Premium و Local-First Playback.
6. Localization، RTL/LTR و تم‌ها.
7. صفحه‌های اصلی و فرعی.
8. انیمیشن‌ها، Shimmer، Shared Transition و Visualizer.
9. تست نهایی، فایل خروجی و ویدئو.

---

## یادداشت نهایی

این چک‌لیست شامل تمام الزامات صریح سند پروژه و همچنین تست‌های قابل انجام برای اطمینان از درست‌بودن پیاده‌سازی آن‌هاست. مواردی که سند جزئیاتشان را آزاد گذاشته، با توضیح جداگانه مشخص شده‌اند تا با الزام مستقیم اشتباه نشوند.
